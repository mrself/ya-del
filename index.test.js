var chai = require('chai'),
	expect = chai.expect,
	assert = chai.assert,
	$ = require('jquery'),
	del = require('./del');

describe('#makeName', function() {
	it('it should work', function() {
		assert(false);
	});
});